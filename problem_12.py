# Generating random number

import random

a = random.randint(1,100) # Gives random number from the range given 
print(f"The random generated number is : {a}")

b = random.random() # Gives random Float number
print(b)